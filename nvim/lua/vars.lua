--> vars.lua

-- Creating a shortcut (instead of typing the whole table name, we simply type g)
local g = vim.g

g.t_co = 256				-- Enables support for 256 colors (for those sweeet sweet themes)
g.background = "dark"		-- Defaulty stick with dark mode pweeease

